This repository contains the implementation of race-detection algorithm used in FoxRacer.

Compile:
	cd Debug
	make clean
	make

Run:
	./Debug/foxracer <trace-file>


Note: This branch uses blocks for ST rules and but disables it for MT rules.
