#include <stdlib.h>
#include <cstdio>

struct node{
  int data;
  struct node* next;
};

int main(){
  // node* head = new node;
  // head->data = 5;
  // head->next = NULL;
  
  // int *a = new int;
  // printf("a contains %p \n", a);
  // *a = 5;
  // delete a;
  // //*a = 8;
  // printf("Got here!(2)");
  // int *b = (int *) malloc(sizeof(int));
  // *b = 12;
  // free(b);
  int a = 5, b = 6, c;
  c = a + b;
  //*b = 16;
}
